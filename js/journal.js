const list=document.getElementById('journal-list');if(list)list.innerHTML=posts.map(p=>`<article class="journal-row"><img src="${p.image}" alt="${p.title}"><div><p class="eyebrow">${p.category} · ${p.date}</p><h2>${p.title}</h2><p>${p.excerpt}</p><a class="text-link" href="article.html?id=${p.id}">Read more →</a></div></article>`).join('');
const target=document.getElementById('article-detail');if(target){const p=posts.find(x=>x.id==new URLSearchParams(location.search).get('id'))||posts[0];document.title=`${p.title} — Raushni Kumari`;target.innerHTML=`<header><p class="eyebrow">${p.category} · ${p.date}</p><h1>${p.title}</h1><p>${p.excerpt}</p></header><img class="article-image" src="${p.image}" alt="${p.title}"><div class="article-body">${p.body.split('\n\n').map(x=>`<p>${x}</p>`).join('')}</div>`}
