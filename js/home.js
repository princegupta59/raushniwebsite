const card=a=>`<a class="art-card" href="artwork.html?id=${a.id}"><div class="art-image"><img loading="lazy" src="${a.image}" alt="${a.title}"></div><div class="card-meta"><h3>${a.title}</h3><p>${a.year} · ${a.medium}</p></div></a>`;
document.getElementById('featured-works').innerHTML=artworks.slice(0,6).map(card).join('');
document.getElementById('featured-collections').innerHTML=collections.slice(0,3).map(c=>`<a class="collection-card" href="collections.html?collection=${encodeURIComponent(c.title)}"><img loading="lazy" src="${c.cover}" alt="${c.title}"><div><p>${artworks.filter(a=>a.collection===c.title).length} works</p><h3>${c.title}</h3><span>Explore collection →</span></div></a>`).join('');
document.getElementById('latest-posts').innerHTML=posts.map(p=>`<article class="post-card"><img loading="lazy" src="${p.image}" alt="${p.title}"><p class="eyebrow">${p.category} · ${p.date}</p><h3>${p.title}</h3><p>${p.excerpt}</p><a class="text-link" href="article.html?id=${p.id}">Read story →</a></article>`).join('');
