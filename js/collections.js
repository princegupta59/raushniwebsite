const selected=new URLSearchParams(location.search).get('collection');document.getElementById('collections-list').innerHTML=collections.map(c=>`<article class="collection-row ${selected===c.title?'selected':''}"><img src="${c.cover}" alt="${c.title}"><div><p class="eyebrow">${artworks.filter(a=>a.collection===c.title).length} artworks</p><h2>${c.title}</h2><p>${c.description}</p><a class="button button-outline" href="gallery.html?collection=${encodeURIComponent(c.title)}">View collection ↗</a></div></article>`).join('');
